TO DO
=====

 * Add a paste listing.
 * Store recent pastes in a session.
 * Add an nginx config file.
 * Unit tests.
 * Make the 404 more generic.
 * Move the controllers to a separate file.
 * Add link colors.
 * Log PHP version.
 * Logo should link back to index.
 * Add view counts to pastes.
 * Save IP address to database when paste is created.
